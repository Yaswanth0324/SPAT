package com.sapt.common.enums;

/**
 * Notification types matching the notifications.type ENUM in DB.
 */
public enum NotificationType {
    SUBMISSION_APPROVED,
    SUBMISSION_REJECTED,
    SUBMISSION_PENDING,
    MENTOR_APPROVED,
    MENTOR_REJECTED,
    HOD_APPROVED,
    HOD_REJECTED,
    SUCCESSION_REQUEST,
    SUCCESSION_APPROVED,
    SUCCESSION_REJECTED,
    SYSTEM_ANNOUNCEMENT
}
