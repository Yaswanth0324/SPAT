package com.sapt.common.enums;

/**
 * Entity type that a notification references.
 * Maps to notifications.reference_type ENUM in DB.
 */
public enum NotificationReferenceType {
    SUBMISSION,
    USER,
    COLLEGE,
    LOG
}
