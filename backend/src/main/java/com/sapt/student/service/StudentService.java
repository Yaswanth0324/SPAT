package com.sapt.student.service;

/**
 * StudentService - Interface for student business logic.
 * TODO (Student Team): Define and implement all student operations here.
 */
public interface StudentService {
    // TODO: StudentProfile getProfile(Long authUserId);
    // TODO: void updateProfile(Long authUserId, StudentDto.UpdateProfileRequest request);
    // TODO: Page<Submission> getMySubmissions(Long authUserId, Pageable pageable);
    // TODO: Integer getTotalPoints(Long authUserId);
}
